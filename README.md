# barrie-pages

One-page shop websites for Barrie / Simcoe trades. Each folder in `sites/` is the shop’s live site, written in that shop’s voice. Start with `sites/jeff-mackie/`. Copy the locked template with `TEMPLATE.md`.

Quote requests post to `quote.php` and email `contact@teamulate.ca`.
